import { SizeconvertPipe } from './sizeconvert.pipe';

describe('SizeconvertPipe', () => {
  it('create an instance', () => {
    const pipe = new SizeconvertPipe();
    expect(pipe).toBeTruthy();
  });
});
